{
  "name": "todo-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "prettier": "prettier --write \"src/**/*.js\" --config \".prettierrc.json\"",
    "explain": "in the above line, we add prettier config, src/**/*.js means find .js file under src and its subfolder, if any"
  },
  "devDependencies": {
    "prettier": "3.6.2",
    "vite": "^7.1.2"
  }
}
